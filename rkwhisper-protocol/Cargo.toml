[package]
name = "rkwhisper-protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
rustix = { version = "1.1.3", features = ["fs", "mm", "net"] }
tokio = { version = "1", features = ["io-util"], optional = true }

[features]
async = ["tokio"]
